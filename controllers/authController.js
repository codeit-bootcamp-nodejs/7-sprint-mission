const bcrypt = require('bcrypt');
const jwt = require('jsonwebtoken');
const prisma = require('../lib/prisma');

exports.signup = async (req, res) => { /* 회원가입 로직 */ };
exports.login = async (req, res) => { /* 로그인 로직 */ };